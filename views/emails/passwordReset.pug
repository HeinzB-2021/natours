extends baseEmail

block content

    p Hi #{firstName},
        p Passwort vergessen? send a patch request with your new password to: #{url}
        table.btn.btn-primary(role='presentation' border='0' cellpadding='0' cellspacing='0')
            tbody
            tr
                td(align='left')
                table(role='presentation' border='0' cellpadding='0' cellspacing='0')
                    tbody
                    tr
                        td
                        a(href=`${url}` target='_blank') Reset Passwort
        p Falls nicht, dann vergiss es. 😃 